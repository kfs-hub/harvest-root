# Server
PORT=3000
NODE_ENV=development
SESSION_SECRET=change-this-to-a-long-random-string

# Email — REQUIRED in production or customers will NOT receive verification codes
# Gmail: enable 2-Step Verification, then create an App Password at
# https://myaccount.google.com/apppasswords
EMAIL_USER=harvestroot2020@gmail.com
EMAIL_PASS=your-16-char-app-password-here
EMAIL_SERVICE=gmail

# Optional custom SMTP (instead of Gmail defaults)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Production (Render / persistent disk)
# DATA_DIR=/var/data
# DB_PATH=/var/data/database.sqlite
